import type { NextConfig } from "next";

// Deliberately empty. No experimental flags, no rewrites, no custom webpack —
// see CLAUDE.md on keeping RSC usage plain.
const nextConfig: NextConfig = {};

export default nextConfig;
