packages:
  - "packages/*"
  - "apps/*"
allowBuilds:
  esbuild: true
  # Native resolver behind eslint-import-resolver-typescript, pulled in
  # transitively by eslint-config-next. Same trust tier as esbuild above.
  unrs-resolver: true
