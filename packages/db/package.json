{
  "name": "@summerice/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "generate": "node guard-host.ts local-only direct && drizzle-kit generate",
    "migrate": "node guard-host.ts local-only direct && drizzle-kit migrate",
    "migrate:prod": "SUMMERICE_ENV=production node guard-host.ts remote-required direct && SUMMERICE_ENV=production drizzle-kit migrate",
    "studio": "node guard-host.ts local-only direct && drizzle-kit studio",
    "seed": "node guard-host.ts local-only direct && node seed.ts",
    "health:realtime": "SUMMERICE_ENV=production node guard-host.ts remote-required direct && SUMMERICE_ENV=production node check-realtime.ts"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "typescript": "6.0.3"
  }
}
